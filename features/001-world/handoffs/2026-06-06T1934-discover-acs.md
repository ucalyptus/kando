---
skill: discover-acs
agent_id: main
started: "2026-06-06T1909"
ended: "2026-06-06T1934"
checkpoint: 2
artifacts:
  - features/001-world/acs.md
exit_criteria:
  - criterion: acs.md exists with content
    verified_by: tool
    met: true
    evidence: "wc -l features/001-world/acs.md → 325 lines; ls -la → 9761 bytes"
  - criterion: All four AC passes covered (happy path, edge cases, errors, cross-cutting)
    verified_by: judgment
    met: true
    evidence: "AC-1–AC-12 happy path; AC-13–AC-21 edge cases; AC-26–AC-27 errors; AC-28–AC-31 cross-cutting; determinism/isolation AC-22–AC-25 (additional pass)"
  - criterion: ACs expressed in domain language, not implementation language
    verified_by: judgment
    met: true
    evidence: "All ACs describe observable World state (objects, relations, context) and caller behavior — no class names, method signatures, or HTTP codes"
  - criterion: Gap decisions captured as ACs with explicit rationale
    verified_by: judgment
    met: true
    evidence: "AC-16 (upsert replace), AC-19 (dangling relations allowed), AC-27 (corrupt snapshot → None) each document the owner's decision and the reasoning"
  - criterion: ac_count and high_priority_count present in frontmatter
    verified_by: tool
    met: true
    evidence: "acs.md frontmatter: ac_count: 31, high_priority_count: 15"
  - criterion: human_action_needed flag set (owner reviews AC contract)
    verified_by: judgment
    met: true
    evidence: "human_action_needed: yes — ACs are a contractual decision; owner must review before CP3 begins"
findings_summary: "31 ACs extracted in reverse-engineer mode; 3 gap decisions resolved by owner (upsert replace, dangling relations, corrupt snapshot → None)"
human_action_needed: yes
human_action_kind: review
recommended_next: "/engineer.atdd to formalize as Given/When/Then specs (after owner review of acs.md)"
status: complete
---

# discover-acs — handoff summary

## What I did

Ran Checkpoint 2 (AC discovery) in reverse-engineer mode for feature `001-world`.
Extracted 31 candidate ACs from `features/world.feature`, `world_steps.py`,
`tests/test_snapshot.py`, and `tests/test_properties.py`, then interviewed the
owner on the three behaviors the existing material left undecided.

## Artifacts produced

- `features/001-world/acs.md` — 31 ACs across happy path, edge cases, errors,
  determinism/isolation, and cross-cutting; YAML frontmatter with counts and
  discovery metadata.

## Findings

Three behaviors the existing tests did not pin down required explicit owner decisions:

| Gap | Decision |
|-----|----------|
| `object.created` for an existing id | Replaces (upsert) — stream is faithful, not a validator |
| `relation.created` with non-existent endpoint ids | Allowed — no referential integrity check; responders enforce business rules |
| Corrupt/malformed snapshot file | Return `None` — snapshot is a cache, not source of truth; caller falls back to replay |

Note: `load_snapshot` currently propagates raw exceptions on corrupt files (AC-27 is
not yet met in code). A small fix is needed: wrap `json.loads` + `_world_from_dict`
in a `try/except Exception` returning `None`.

No domain checklists matched (not a web, auth, deploy, or migration feature).

## Human action needed?

Yes — review. ACs are an owner contract: you own what behaviors must work.
Review `features/001-world/acs.md` before CP3 begins. In particular, confirm the
three gap decisions (AC-16, AC-19, AC-27) look right.

## Recommended next step

After review: `/engineer.atdd` to formalize `acs.md` into Given/When/Then specs
(`spec.md`).

Also: patch `load_snapshot` to return `None` on any exception (AC-27) before CP3
so the spec can be written against the correct behavior.
